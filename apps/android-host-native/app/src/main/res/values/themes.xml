<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fully transparent, no-chrome theme for ProjectionRequestActivity so the
         token-grab is invisible except for the system capture dialog itself. -->
    <style name="Theme.Transparent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
