{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Transparent annotation overlay window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
